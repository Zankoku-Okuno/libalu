// DEPS: <stddef.h> <stdint.h> <stdbool.h>

// FIXME move this struct definition to a different file
struct divmod_@T {
    @TYPE div;
    @TYPE mod;
};

inline
struct divmod_@T divmod_@Tf(@TYPE a, @TYPE b, bool* sticky_bit);

inline
@TYPE div_@Tf(@TYPE a, @TYPE b, bool* sticky_bit);

inline
@TYPE mod_@Tf(@TYPE a, @TYPE b, bool* sticky_bit);
