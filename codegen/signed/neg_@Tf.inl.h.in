// DEPS: <stddef.h> <stdint.h> <stdbool.h>

inline
@TYPE neg_@Tf(@TYPE a, bool* sticky_bit) {
    if (a == @MIN) {
        *sticky_bit |= true;
        return a;
    }
    else {
        return -a;
    }
}
