typedef struct TVAR`_carry' {
    TVAR low;
    TVAR high;
} TVAR`_carry';

typedef struct TVAR`_divmod' {
    TVAR div;
    TVAR mod;
} TVAR`_divmod';

typedef struct TVAR`_quotrem' {
    TVAR quot;
    TVAR rem;
} TVAR`_quotrem';

